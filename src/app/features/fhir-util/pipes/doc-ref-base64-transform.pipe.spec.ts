import { DocRefBase64TransformPipe } from './doc-ref-base64-transform.pipe';

describe('DocRefBase64TransformPipe', () => {
  it('create an instance', () => {
    const pipe = new DocRefBase64TransformPipe();
    expect(pipe).toBeTruthy();
  });
});
