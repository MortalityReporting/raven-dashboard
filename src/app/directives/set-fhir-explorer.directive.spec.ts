import { SetFhirExplorerDirective } from './set-fhir-explorer.directive';

describe('SetFhirExplorerDirective', () => {
  it('should create an instance', () => {
    const directive = new SetFhirExplorerDirective();
    expect(directive).toBeTruthy();
  });
});
